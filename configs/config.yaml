data:
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  cache_dir: "data/cache_embeddings"
  outputs_dir: "data/outputs"
models:
  clip_model_name: "openai/clip-vit-base-patch32"
sieve:
  batch_size: 32
  device: "cpu"   # change to "cuda" later if available
